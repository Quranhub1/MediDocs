rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // RESOURCES_STUDYPEDIA/{resourceId}/courses/{courseId}/semesters/{semesterId}/courseunits/{unitId}/units/{unitDocId}
    match /RESOURCES_STUDYPEDIA/{resourceId} {
      allow read: if true;
      allow write: if request.auth != null && request.auth.token.email == 'admin@studypedia.ug';
      
      match /courses/{courseId} {
        allow read: if request.auth != null;
        allow write: if request.auth != null && request.auth.token.email == 'admin@studypedia.ug';
        
        match /semesters/{semesterId} {
          allow read: if request.auth != null;
          allow write: if request.auth != null && request.auth.token.email == 'admin@studypedia.ug';
          
          match /courseunits/{unitId} {
            allow read: if request.auth != null;
            allow write: if request.auth != null && request.auth.token.email == 'admin@studypedia.ug';
            
            match /units/{unitDocId} {
              allow read: if request.auth != null;
              allow write: if request.auth != null && request.auth.token.email == 'admin@studypedia.ug';
            }
          }
        }
      }
    }
    
    // User payments
    match /user_payments/{userId} {
      allow read: if request.auth != null && request.auth.uid == userId 
                  || request.auth.token.email == 'admin@studypedia.ug';
      allow write: if request.auth != null && request.auth.token.email == 'admin@studypedia.ug';
    }
    
    // Users
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
